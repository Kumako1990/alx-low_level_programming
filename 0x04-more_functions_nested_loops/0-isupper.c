#include "main.h"

/**
 * isupper Checks for uppercase
 * the character to check
 * Return 1 if character is uppercase, o otherwise
 * */
int _isupper(int c)
{
		if (c >= 'A' && c <= 'Z')
					return (1);

			else
						return (0);
}
